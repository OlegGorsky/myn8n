version: '3.7'

services:
  n8n:
    build: .
    ports:
      - 5678:5678
    environment:
      - DB_TYPE=sqlite
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=pass
    volumes:
      - .n8n:/home/node/.n8n
